services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: octa
      POSTGRES_PASSWORD: octa_dev
      POSTGRES_DB: octa_dashboard
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
